//! perk/v1 wire DTOs. JSON field names are exactly the snake-case names
//! from the normative plugin contract (docs/plugins.md in the host repo).

pub mod capabilities;
pub mod request;
pub mod service;
pub mod write;
